ru-ru: Для работоспособности Slash команд необходимо распаковать приложенный архив в папку с версией Python и выбрать эту версию в качестве интерпретатора.
en-us: For Slash commands to work, you need to unpack the attached archive into a folder with the Python version and select this version as the interpreter.
de: Damit die Slash-Befehle funktionieren, müssen Sie das angehängte Archiv in einen Python-Versionsordner entpacken und diese Version als Interpreter auswählen.
fr: Pour que les commandes Slash fonctionnent correctement, vous devez décompresser l'archive jointe dans un dossier contenant la version Python et sélectionner cette version comme interpréteur.
ja: スラッシュコマンドを動作させるには、添付されたアーカイブをPythonバージョンのフォルダに解凍し、このバージョンをインタプリタとして選択する必要があります。
zh: 要使斜杠命令工作，您需要将附加的存档解压缩到具有Python版本的文件夹中，并选择此版本作为解释器。
